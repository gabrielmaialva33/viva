%% VIVA HRR FFT - Fast Fourier Transform for Holographic Reduced Representations
%% Stub implementation - falls back to Pure Gleam naive convolution

-module(viva_hrr_fft).
-export([circular_conv/2]).

%% Circular convolution via FFT
%% This is a stub - returns empty list to trigger fallback
%% Real implementation would use Nx/EXLA or native FFT
circular_conv(_A, _B) ->
    [].
