defmodule VivaCommon do
  @moduledoc """
  Common utilities shared across VIVA apps.

  This app provides foundational modules that are needed by both
  viva_core (Soul) and viva_bridge (Body) without creating circular dependencies.
  """
end
